= render partial: 'shared/flashes'
= simple_form_for :user, url: sessions_path do |f|
  = f.input :username, label: t('sessions.username')
  = f.input :password, label: t('sessions.password')
  = f.button :submit, t('sessions.new_session')

= link_to t('sessions.new_user'), new_user_path
